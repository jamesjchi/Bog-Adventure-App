<h1>New Creature</h1>

<%= bootstrap_form_for @creature do |t| %>
  <div>
    
    <%= t.text_field :name %>
  </div>

  <div>
        <%= t.text_area :description %>
  </div>

  <%= t.submit %>
<% end %>
